import { SubjectCodePipe } from './subject-code.pipe';

describe('SubjectCodePipe', () => {
  it('create an instance', () => {
    const pipe = new SubjectCodePipe();
    expect(pipe).toBeTruthy();
  });
});
